export class OverallUserPoint {
    profileId: number;
    rank: number;
    userName: string;
    points: number;
    transfersLeft: number;

    // constructor(values: Object = {}) {
    //     Object.assign(this, values);
    // }
}
