import { Tournament } from './tournament';

export class TournamentPhase {
    phaseId: number;
    phaseNo: number;
    tournamentId: number;
    noOfTransfersAllowed: number;
    startDate: string;
    endDate: string;
}
