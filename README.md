# cricket_rivalries

## Local setup

* Update `api/src/main/resources/application.properties` with local db details
* Run  `mvn clean install` from project root directory
* Run `mvn spring-boot:run` from /api directory
